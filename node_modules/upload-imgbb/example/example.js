const image = require('../index.js'); // or you can require('upload-imgbb');

(async () => {
  try {
    const imageUrl = await image.uploadLink('https://example.com/image');
    console.log(imageUrl);
  } catch (error) {
    console.error(error);
  }
})();